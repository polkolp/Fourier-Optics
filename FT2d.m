%FT2d% shows the Fourier transform of a delta functionn=128; %array sizex=1,y=2, % location of delta function% change these numbers to get different fringe patternsg=zeros(n,n);row=y+1;col=x+1;g(row,col)=1; % put the delta-function thereG=fftshift(fftn((g))); % Fourier transform itfigure(3);showp(real(G)-min(real(G(:))),1,2); % the real partfigure(4);showp(imag(G)-min(imag(G(:))),1,2); % the imaginary partfigure(5);showp(arg(G)+pi,1,2); % the phasereturn